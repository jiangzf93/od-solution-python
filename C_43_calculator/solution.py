class Solution(object):
    def solve(self, inputStr):
        sIndex = 0
        dStr = ''
        nums = []
        ops = []
        while sIndex < len(inputStr):
            s = inputStr[sIndex]
            if s.isdigit():
                
                    
        return 